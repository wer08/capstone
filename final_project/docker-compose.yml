version: "3.9"

services:
  db:
    image: postgres
    environment:
      - POSTRES_DB=fitness
      - POSTGRES_USER=wer08
      - POSTGRES_PASSWORD=M0rg0th&CO
    ports:
      - "5432:5432"
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code/
    ports:
      - "8000:8000"
    depends_on:
      - db
